<%= bootstrap_form_for @task do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :details %>
  <% unless @task.title == nil %>
    <%= f.check_box :completed %>
  <% end %>
  <%= f.submit %>
<% end %>
